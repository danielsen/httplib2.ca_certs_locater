from setuptools import setup, find_packages

setup(
    name="ca_certs_locater",
    version="0.1.0",
    description="ca_certs_locater httplib2 plugin",
    author="Dan Nielsen",
    author_email="dnielsen@reachmail.com",
    packages=find_packages()
)
